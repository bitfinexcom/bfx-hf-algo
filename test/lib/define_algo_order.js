/* eslint-env mocha */
'use strict'

const assert = require('assert')
const _isFunction = require('lodash/isFunction')
const defineAlgoOrder = require('../../lib/define_algo_order')

// TODO: stub for coverage results
describe('defineAlgoOrder', () => {
  assert.ok(_isFunction(defineAlgoOrder))
})
